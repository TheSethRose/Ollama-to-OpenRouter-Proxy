# Example environment variables for ollama-proxy

# Required: Your OpenRouter API key
OPENROUTER_API_KEY=sk-...

# Optional: Host and port for the FastAPI server (can also be set via CLI)
HOST=0.0.0.0
PORT=11434

# Optional: Path to the model filter file
MODELS_FILTER=models-filter.txt
